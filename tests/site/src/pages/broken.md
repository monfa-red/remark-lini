# Broken

A block that cannot compile becomes an error box, and the build still finishes.

```lini
|nonesuch| "no such primitive"
```

An ordinary fence, so the page also proves the host's own code blocks still
work beside ours.

```js
const x = 1;
```
